apiVersion: apps/v1
kind: Deployment
metadata:
  name: cbz-deployment
  labels:
    app: cbz-app1
spec:
  replicas: 3
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: cbz-app1
  template:
    metadata:
      labels:
        app: cbz-app1
    spec:
      containers:
      - name: cbz-app1
        image: 692701344220.dkr.ecr.eu-west-3.amazonaws.com/cbz:v1
        imagePullPolicy: Always
        ports:
        - containerPort: 80
